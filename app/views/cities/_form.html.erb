<%= form_for @city, :html => { :class => "form-horizontal city" } do |f| %>

    <% if @city.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @city.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :mm_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :mm_name, :class => 'form-control' %>
    </div>
    <%= error_span(@city[:mm_name]) %>
  </div>
  <div class="control-group">
    <%= f.label :en_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :en_name, :class => 'form-control' %>
    </div>
    <%= error_span(@city[:en_name]) %>
  </div>
  <div class="control-group">
    <%= f.label :latitude, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :latitude, :class => 'form-control' %>
    </div>
    <%= error_span(@city[:latitude]) %>
  </div>
  <div class="control-group">
    <%= f.label :longitude, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :longitude, :class => 'form-control' %>
    </div>
    <%= error_span(@city[:longitude]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            cities_path, :class => 'btn btn-default' %>

<% end %>
